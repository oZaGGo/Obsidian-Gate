spring.application.name=mc-panel

# MySQL connection
spring.datasource.url=jdbc:mysql://localhost:3306/obsidiangate_db?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=admin
spring.datasource.password=1246354

# Strategy for schema generation (create, update, validate, none)
spring.jpa.hibernate.ddl-auto=update

# Show SQL statements in the console
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Specify the Hibernate dialect for MySQL
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

project.version=@project.version@
